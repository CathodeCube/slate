/**
 * Slate module — public API.
 *
 * Exports the `ISlate` interface and `Slate` implementation.
 */
export type { ISlate, ResolveResult, SlateError } from "./ISlate";
export { Slate, type SlateOptions } from "./Slate";
