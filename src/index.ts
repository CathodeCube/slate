/**
 * Slate — CLI entry point.
 *
 * When run via `bun run start` or the `slate` binary, this initializes the
 * Commander CLI and delegates to the command handlers.
 */
import { main } from "src/cli";

main();
